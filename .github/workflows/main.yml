name: prettier
'on':
 - pull
jobs:
   build:
   runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: |
        npm install
    - name: Run Prettier
      run: |
        npm run check
