name: CI
'on':
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install modules
      run: |
        pwd
        ls
        npm install
    - name: Run Prettier
      run: |
        npm run check
